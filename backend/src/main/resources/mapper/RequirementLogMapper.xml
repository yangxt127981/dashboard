<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dashboard.mapper.RequirementLogMapper">

    <resultMap id="logMap" type="com.dashboard.entity.RequirementLog">
        <id property="id" column="id"/>
        <result property="requirementId" column="requirement_id"/>
        <result property="operator" column="operator"/>
        <result property="operationType" column="operation_type"/>
        <result property="beforeContent" column="before_content"/>
        <result property="afterContent" column="after_content"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.dashboard.entity.RequirementLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO requirement_log (requirement_id, operator, operation_type, before_content, after_content)
        VALUES (#{requirementId}, #{operator}, #{operationType}, #{beforeContent}, #{afterContent})
    </insert>

    <select id="findByRequirementId" resultMap="logMap">
        SELECT * FROM requirement_log
        WHERE requirement_id = #{requirementId}
        ORDER BY created_at DESC
    </select>

</mapper>
